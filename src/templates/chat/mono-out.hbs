{{!-- Rien n'est bloqué ici : un trait entamé ne donne déjà plus de dé, donc
l'appareil a cessé tout seul d'apporter quoi que ce soit. Cette carte ne fait
que signaler le moment et passer la main. --}}
<div class="cowboy-mono-out-card">
  <header>
    <img src="{{ img }}" alt="" width="48" height="48" />
    <h3>{{ name }}</h3>
  </header>
  <p class="cowboy-mono-out-title">{{localize 'COWBOY.mono.outOfChase'}}</p>
  <p class="cowboy-mono-out-hint">{{localize 'COWBOY.mono.outOfChaseHint'}}</p>
</div>
